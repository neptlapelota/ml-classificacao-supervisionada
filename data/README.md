# Pasta `data`

Coloque aqui os datasets usados para executar o notebook localmente.

Para o exercício de diabetes, o notebook espera o arquivo:

```text
dataset_37_diabetes.arff
```

Por organização, recomenda-se ajustar o caminho no notebook para:

```python
data, meta = arff.loadarff('../data/dataset_37_diabetes.arff')
```

Os datasets não foram incluídos neste repositório para evitar versionamento desnecessário de dados locais.
